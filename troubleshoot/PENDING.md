# Troubleshoot assets pending

The following files are expected to be added in later sessions:

- `prepare.sh`
- `broken-app.yaml`
- `verify.sh`
- `smoke-job.yaml`

This placeholder is intentionally minimal so the bootstrap can be verified before the troubleshooting content is added.
